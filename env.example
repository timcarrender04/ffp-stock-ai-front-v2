# ============================================
# FRONTEND SUPABASE CONFIGURATION (Public)
# ============================================
# Used by browser/client-side code
# For local dev: Supabase Kong forwards PostgREST on http://localhost:54321
# For production: Point to your public Supabase HTTPS URL
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321

# Get this from Supabase dashboard or docker logs (must match your JWT secret)
# docker-compose logs supabase_kong_supabase-ffp | grep -i anon
NEXT_PUBLIC_SUPABASE_ANON_KEY=<replace-with-your-local-supabase-anon-key>

# ============================================
# BACKEND API CONFIGURATION
# ============================================
# The FastAPI service running on port 8000
# For local dev: Use http://localhost:8000
# For production: Use https://api.ffpstock.app (or your public backend URL)
NEXT_PUBLIC_API_URL=http://localhost:8000

# AI Chat WebSocket/REST service (runs on port 8001 locally)
# For production: Point to your public ai-chat service (e.g., https://ai-chat.ffpstock.app)
NEXT_PUBLIC_AI_CHAT_URL=http://localhost:8001

# ============================================
# SERVER-SIDE SUPABASE CONFIGURATION (Private)
# ============================================
# Used by Next.js server components and API routes
# For local dev: http://localhost:54321 (Supabase Kong)
# For Docker: Use the internal Docker network address (e.g., http://supabase_kong_supabase-ffp:8000)
SUPABASE_URL=http://localhost:54321

# Get the service role key from Supabase dashboard
# This is more privileged than the anon key - keep it private!
SUPABASE_SERVICE_ROLE_KEY=<replace-with-your-supabase-service-role-key>

# Kong API Gateway (usually same as SUPABASE_URL)
# For local dev: http://localhost:54321
# For Docker: Use internal Docker network address
SUPABASE_KONG_URL=http://localhost:54321

# Direct PostgREST base URL (used by moonshot API routes)
# Local dev uses http://localhost:54321, Docker can use http://supabase_kong_supabase-ffp:8000
POSTGREST_URL=http://localhost:54321

# ============================================
# AI CHAT BACKEND (Server-side fetch + WebSocket proxy)
# ============================================
# Used by Next.js API routes to talk to the AI chat FastAPI service
# Local dev: http://localhost:8001 (maps to /api/chat/* and /ws/chat)
# Docker: http://ai_agent_chat:8001
AI_CHAT_API_URL=http://localhost:8001

# ============================================
# DISCORD NOTIFICATIONS
# ============================================
# Discord webhook URL for Moonshot Top 25 new symbol notifications
# Get this from Discord channel settings > Integrations > Webhooks
# Notifications are only sent during trading hours (9 AM - 5 PM EST)
DISCORD_MOONSHOT_WEBHOOK_URL=https://discordapp.com/api/webhooks/1440145857809875045/RihgbXYy1zEU9buWvq6S-ixz8Qlb1cioUo7gCUa6DvpMooiRWMGxM-4KvTD8KhjGpoC9

